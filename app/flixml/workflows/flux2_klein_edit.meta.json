{
  "id": "flux2_klein_edit",
  "name": "FLUX.2 Klein Reference Edit",
  "description": "Generate a new image from a reference image and a plain-English instruction. FLUX.2 Klein 4B reads the reference as conditioning and samples a fresh frame, so unlike img2img it can change pose, wardrobe and setting while holding the subject's identity. Say what changes and what stays.",
  "task": "image-to-image",
  "output_type": "image",
  "requirements": {
    "vram_gb": 8,
    "model_files_gb": 8.3,
    "workflow_type": "flux2",
    "supports_lora": true,
    "requires_image": true
  },
  "params": {
    "image": {
      "type": "str",
      "required": true,
      "description": "Reference image filename"
    },
    "prompt": {
      "type": "str",
      "required": true,
      "description": "An instruction, not a scene description. Name the change, then name what stays: 'Change her outfit to a black leather coat and place her on a castle rampart at dusk. Keep her face and hair exactly as they are.' Never re-describe the subject's face, hair or body — the reference carries them, and describing them again makes the model draw its own version instead."
    },
    "seed": {
      "type": "int",
      "required": false,
      "description": "Random seed (auto if omitted)"
    },
    "lora_name": {
      "type": "str",
      "default": "",
      "description": "Optional LoRA file in the node's loras folder. Leave empty and the slot is removed from the graph entirely. A FLUX.2 LoRA is built for one variant: a Klein 4B LoRA does not load on Klein 9B or on FLUX.2-dev, and a dev LoRA does not load here — the residual stream is 3072 wide on 4B against 6144 on dev, so the tensors do not fit. Match the LoRA's stated base model to the weights in the unet param."
    },
    "lora_strength": {
      "type": "float",
      "default": 1.0,
      "min": 0.0,
      "max": 2.0,
      "description": "Weight of the first LoRA. Ignored when lora_name is empty."
    },
    "lora_name_2": {
      "type": "str",
      "default": "",
      "description": "Second LoRA, chained after the first — for stacking a concept LoRA on top of a likeness or style one. Same variant rule as lora_name."
    },
    "lora_strength_2": {
      "type": "float",
      "default": 1.0,
      "min": 0.0,
      "max": 2.0,
      "description": "Weight of the second LoRA. Ignored when lora_name_2 is empty."
    },
    "steps": {
      "type": "int",
      "default": 4,
      "min": 1,
      "max": 50,
      "description": "Klein is distilled to 4 steps; raise it only when running undistilled FLUX.2 weights through the unet param"
    },
    "cfg": {
      "type": "float",
      "default": 1.0,
      "description": "Distilled Klein runs at 1.0. Raising it fights the distillation"
    },
    "sampler": {
      "type": "str",
      "default": "euler"
    },
    "reference_megapixels": {
      "type": "float",
      "default": 1.0,
      "min": 0.25,
      "max": 4.0,
      "description": "The reference is scaled to this many megapixels before encoding, and the output inherits its size"
    },
    "unet": {
      "type": "str",
      "default": "flux-2-klein-4b-fp8.safetensors"
    },
    "clip": {
      "type": "str",
      "default": "qwen_3_4b_fp4_flux2.safetensors"
    },
    "vae": {
      "type": "str",
      "default": "flux2-vae.safetensors"
    }
  },
  "compatible_providers": [
    "local",
    "cloud_serverless"
  ],
  "examples": []
}
