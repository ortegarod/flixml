{
  "id": "flux2_klein",
  "name": "FLUX.2 Klein Image",
  "description": "Text-to-image with FLUX.2 Klein 4B. High-fidelity stills from a prompt, with two optional LoRA slots.",
  "task": "text-to-image",
  "output_type": "image",
  "requirements": {
    "vram_gb": 8,
    "model_files_gb": 8.3,
    "workflow_type": "flux2",
    "supports_lora": true,
    "notes": "Defaults to FLUX.2 Klein 4B fp8 (4.07 GB) plus the fp4 Qwen3-4B encoder (3.85 GB), about 8.3 GB of weights with the VAE. Klein is the distilled line and renders in 4 steps. TESTED 2026-09-19 at 1024x1024, 4 steps, same prompt and seed on both cards: fp8 on an 8 GB RTX 2060 Super took 29 s, bf16 (7.75 GB) on a 12 GB RTX 4070 Ti took 37 s, and the two outputs are visually identical — fp8 costs nothing here. The unet and clip params reach any FLUX.2 weights present on the node; FLUX.2-dev (35 GB fp8 plus a 12 GB Mistral encoder) wants 24 GB VRAM and spills badly below that — 32 min for a single image on the 4070 Ti — so dev belongs on flux2_dev_lora, the only workflow the dev-trained LoRAs load into."
  },
  "params": {
    "prompt": {
      "type": "str",
      "required": true,
      "description": "Natural language sentences, not tags — the opposite of an SDXL prompt. Subject, then action, then style, then context, in that order: word order is weight, so lead with the subject and close with atmosphere. 30-80 words suits most shots. 'A businessman in a charcoal grey suit resting his arms on a bamboo railing at a secluded beach, illustrated in a vintage woodblock print style, calm turquoise water under a hazy afternoon sky.' FLUX.2 has no negative prompt and this graph has no node for one, so describe what you want instead of what you don't — 'sharp focus throughout', not 'not blurry'. For photorealism, name a camera, lens or film stock rather than saying 'professional'."
    },
    "lora_name": {
      "type": "str",
      "default": "",
      "description": "Optional LoRA file in the node's loras folder. Leave empty and the slot is removed from the graph entirely. A FLUX.2 LoRA is built for one variant: a Klein 4B LoRA does not load on Klein 9B or on FLUX.2-dev, and a dev LoRA does not load here — the residual stream is 3072 wide on 4B against 6144 on dev, so the tensors do not fit. Match the LoRA's stated base model to the weights in the unet param."
    },
    "lora_strength": {
      "type": "float",
      "default": 1.0,
      "min": 0.0,
      "max": 2.0,
      "description": "Weight of the first LoRA. Ignored when lora_name is empty."
    },
    "lora_name_2": {
      "type": "str",
      "default": "",
      "description": "Second LoRA, chained after the first — for stacking a concept LoRA on top of a likeness or style one. Same variant rule as lora_name."
    },
    "lora_strength_2": {
      "type": "float",
      "default": 1.0,
      "min": 0.0,
      "max": 2.0,
      "description": "Weight of the second LoRA. Ignored when lora_name_2 is empty."
    },
    "width": {
      "type": "int",
      "default": 832,
      "description": "Output width"
    },
    "height": {
      "type": "int",
      "default": 832,
      "description": "Output height"
    },
    "seed": {
      "type": "int",
      "required": false,
      "description": "Random seed (auto if omitted)"
    },
    "steps": {
      "type": "int",
      "default": 4,
      "min": 1,
      "max": 50,
      "description": "Klein is distilled to 4 steps; raise it only when running undistilled FLUX.2 weights through the unet param"
    },
    "sampler": {
      "type": "str",
      "default": "euler"
    },
    "guidance": {
      "type": "float",
      "default": 4.0,
      "description": "FLUX guidance scale"
    },
    "unet": {
      "type": "str",
      "default": "flux-2-klein-4b-fp8.safetensors"
    },
    "clip": {
      "type": "str",
      "default": "qwen_3_4b_fp4_flux2.safetensors"
    },
    "vae": {
      "type": "str",
      "default": "flux2-vae.safetensors"
    }
  },
  "compatible_providers": [
    "local",
    "cloud_serverless"
  ],
  "examples": [
    {
      "prompt": "A weathered fishing boat moored at a stone quay before dawn, ropes coiled on the deck, shot on Kodak Portra 400 at 35mm f/2, soft blue pre-sunrise light and still water holding the reflection.",
      "width": 1024,
      "height": 1024,
      "steps": 4
    }
  ]
}
