{
  "id": "qwen_image_21",
  "name": "Qwen-Image 2.1",
  "description": "Qwen-Image 2.1 makes an image from a prompt. To edit from reference images, use qwen_image_21_edit. Qwen Research License: non-commercial use only.",
  "task": "text-to-image",
  "output_type": "image",
  "requirements": {
    "vram_gb": 12,
    "model_files_gb": 17.3,
    "workflow_type": "qwen_image_21",
    "supports_lora": false,
    "requires_image": false
  },
  "params": {
    "prompt": {
      "type": "str",
      "required": true,
      "description": "Describe the picture. Put any text that should appear in the image in quotes."
    },
    "negative_prompt": {
      "type": "str",
      "default": "",
      "description": "Only read when cfg is above 1."
    },
    "width": {
      "type": "int",
      "default": 1024,
      "description": "Multiples of 32. Qwen's listed sizes run from 2048x2048 at 1:1 to 2752x1536 at 16:9."
    },
    "height": {
      "type": "int",
      "default": 1024,
      "description": "Multiples of 32."
    },
    "seed": {
      "type": "int",
      "required": false,
      "description": "Random seed (auto if omitted)"
    },
    "steps": {
      "type": "int",
      "default": 25,
      "min": 1,
      "max": 60,
      "description": "ComfyUI's template runs 25; Qwen's own examples run 40."
    },
    "cfg": {
      "type": "float",
      "default": 1.0,
      "description": "Keep at 1 for the official path. Raise it only to use a negative prompt."
    },
    "sampler": {
      "type": "str",
      "default": "euler"
    },
    "scheduler": {
      "type": "str",
      "default": "simple"
    },
    "unet": {
      "type": "str",
      "default": "qwen_image_2.1_int8_convrot.safetensors"
    },
    "clip": {
      "type": "str",
      "default": "qwen3vl_8b_int8_convrot.safetensors",
      "description": "Text encoder file on the node. The default is the int8 build; qwen3vl_8b_w4a8.safetensors (6.31 GB) is a smaller one from the same repo for nodes short on system RAM."
    },
    "vae": {
      "type": "str",
      "default": "qwen_image_2.1_vae_bf16.safetensors"
    }
  },
  "compatible_providers": [
    "local"
  ],
  "examples": [
    {
      "prompt": "A hand-painted wooden sign on a market stall that reads \"FRESH FIGS 3 FOR $2\" in chalky white letters, crates of figs below it, early morning side light, shallow depth of field.",
      "width": 1024,
      "height": 1024
    }
  ]
}
